<Window x:Class="puntoDeVenta.Views.BuscadorProductosWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Buscador de Productos" Height="500" Width="700"
        WindowStartupLocation="CenterScreen" ResizeMode="NoResize" WindowStyle="None"
        Background="White" BorderBrush="#3F51B5" BorderThickness="2">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="🔍 Catálogo de Productos" FontSize="20" FontWeight="Bold" Foreground="#3F51B5" Margin="0,0,0,15"/>

        <Border Grid.Row="1" Background="#F5F5F5" CornerRadius="5" Padding="5" Margin="0,0,0,15">
            <StackPanel>
                <TextBlock Text="Escribe para buscar (Nombre o Código):" Foreground="Gray" FontSize="12" Margin="5,0,0,2"/>
                <TextBox x:Name="txtFiltro" FontSize="16" Background="Transparent" BorderThickness="0" 
                         TextChanged="TxtFiltro_TextChanged"  
                         VerticalContentAlignment="Center" Height="30"/>
            </StackPanel>
        </Border>

        <DataGrid x:Name="gridProductos" Grid.Row="2" AutoGenerateColumns="False" IsReadOnly="True"
                  HeadersVisibility="Column" GridLinesVisibility="Horizontal" Background="White"
                  MouseDoubleClick="GridProductos_MouseDoubleClick">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Código" Binding="{Binding CodigoBarras}" Width="100"/>
                <DataGridTextColumn Header="Nombre" Binding="{Binding Nombre}" Width="*"/>
                <DataGridTextColumn Header="Stock" Binding="{Binding Stock}" Width="80" FontWeight="Bold"/>
                <DataGridTextColumn Header="Precio" Binding="{Binding PrecioVenta, StringFormat=C2}" Width="100" FontWeight="Bold" Foreground="Green"/>
            </DataGrid.Columns>

            <DataGrid.InputBindings>
                <KeyBinding Key="Return" Command="{Binding SelectCommand}" />
            </DataGrid.InputBindings>
        </DataGrid>

        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,15,0,0">
            <TextBlock Text="Doble Click o Enter para seleccionar | ESC para salir" VerticalAlignment="Center" Foreground="Gray" Margin="0,0,20,0"/>

            <Button Content="Cancelar" IsCancel="True" Padding="15,8" Background="#EEEEEE" BorderThickness="0" Margin="0,0,10,0"/>
            <Button Content="✅ Seleccionar" Click="BtnSeleccionar_Click" Padding="15,8" Background="#3F51B5" Foreground="White" FontWeight="Bold" BorderThickness="0"/>
        </StackPanel>
    </Grid>
</Window>