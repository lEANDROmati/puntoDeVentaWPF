<UserControl x:Class="puntoDeVenta.Views.InventarioView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="1000" Background="#F5F7FA">

    <UserControl.Resources>
        <Style x:Key="BtnGrilla" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Padding" Value="5"/>
        </Style>
    </UserControl.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Gestión de Inventario" FontSize="24" FontWeight="Bold" Foreground="#3F51B5" Margin="0,0,0,20"/>

        <Grid Grid.Row="1" Margin="0,0,0,20">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <Border Grid.Column="0" Background="White" CornerRadius="5" Padding="5" Margin="0,0,20,0" BorderBrush="#E0E0E0" BorderThickness="1" RenderTransformOrigin="0.497,0.503">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock Text="🔍" VerticalAlignment="Center" Margin="5,0,10,0" Foreground="Gray"/>
                    <TextBox Text="{Binding TextoBusqueda, UpdateSourceTrigger=PropertyChanged}" 
                             BorderThickness="0" VerticalContentAlignment="Center" FontSize="14"
                             Background="Transparent"/>
                </Grid>
            </Border>

            <StackPanel Grid.Column="1" Orientation="Horizontal">
                <Button Command="{Binding NuevoProductoCommand}" Content="+ Nuevo" Background="#3F51B5" Foreground="White" Padding="15,8" Margin="5,0"/>
                <Button Command="{Binding IrAMaestrosCommand}" Content="⚙️ Maestros" Background="#607D8B" Foreground="White" Padding="15,8" Margin="5,0"/>
                <Button Command="{Binding ExportarExcelCommand}" Content="📊 Exportar" Background="#4CAF50" Foreground="White" Padding="15,8" Margin="5,0"/>
            </StackPanel>
        </Grid>

        <DataGrid Grid.Row="2" ItemsSource="{Binding Productos}" AutoGenerateColumns="False" 
                  CanUserAddRows="False" IsReadOnly="True"
                  HeadersVisibility="Column" GridLinesVisibility="Horizontal"
                  Background="White" BorderThickness="0" RowHeight="45"
                  HorizontalGridLinesBrush="#EEEEEE">

            <DataGrid.Columns>
                <DataGridTextColumn Header="Cód." Binding="{Binding CodigoBarras}" Width="80" Foreground="Gray"/>
                <DataGridTextColumn Header="Producto" Binding="{Binding Nombre}" Width="*" FontWeight="SemiBold"/>
                <DataGridTextColumn Header="Categoría" Binding="{Binding Categoria}" Width="120"/>
                <DataGridTextColumn Header="Costo" Binding="{Binding PrecioCompra, StringFormat=${0:N2}}" Width="90" Foreground="#757575"/>
                <DataGridTextColumn Header="Venta" Binding="{Binding PrecioVenta, StringFormat=${0:N2}}" Width="90" Foreground="#2E7D32" FontWeight="Bold"/>
                <DataGridTextColumn Header="Margen" Binding="{Binding MargenGanancia, StringFormat={}{0:P1}}" Width="80"/>

                <DataGridTemplateColumn Header="Stock" Width="80">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <TextBlock>
                                <Run Text="{Binding Stock}" FontWeight="Bold"/>
                                <Run Text="{Binding Unidad}" FontSize="10" Foreground="Gray"/>
                            </TextBlock>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>

                <DataGridTemplateColumn Header="Estado" Width="130">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="10,0">
                                <Ellipse x:Name="dot" Width="10" Height="10" Margin="0,0,8,0" Fill="#E0E0E0"/>

                                <TextBlock Text="{Binding EstadoStock}" Foreground="#455A64" FontSize="11" FontWeight="SemiBold" VerticalAlignment="Center"/>
                            </StackPanel>

                            <DataTemplate.Triggers>
                                <DataTrigger Binding="{Binding EstadoStock}" Value="STOCK BAJO">
                                    <Setter TargetName="dot" Property="Fill" Value="#F44336"/>
                                </DataTrigger>
                                <DataTrigger Binding="{Binding EstadoStock}" Value="STOCK MEDIO">
                                    <Setter TargetName="dot" Property="Fill" Value="#FF9800"/>
                                </DataTrigger>
                                <DataTrigger Binding="{Binding EstadoStock}" Value="STOCK ALTO">
                                    <Setter TargetName="dot" Property="Fill" Value="#4CAF50"/>
                                </DataTrigger>
                            </DataTemplate.Triggers>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>

                <DataGridTemplateColumn Header="Acciones" Width="90">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal">
                                <Button Content="✏️" ToolTip="Editar" Style="{StaticResource BtnGrilla}"
                                        Command="{Binding DataContext.EditarProductoCommand, RelativeSource={RelativeSource AncestorType=UserControl}}" 
                                        CommandParameter="{Binding}"/>
                                <Button Content="🗑️" ToolTip="Eliminar" Style="{StaticResource BtnGrilla}" Foreground="Red"
                                        Command="{Binding DataContext.EliminarProductoCommand, RelativeSource={RelativeSource AncestorType=UserControl}}" 
                                        CommandParameter="{Binding}"/>
                            </StackPanel>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>