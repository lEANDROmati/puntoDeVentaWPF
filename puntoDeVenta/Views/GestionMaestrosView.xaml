<UserControl x:Class="puntoDeVenta.Views.GestionMaestrosView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="500" d:DesignWidth="800" Background="White">

    <UserControl.Resources>
        <Style TargetType="TextBox">
            <Setter Property="Padding" Value="5"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Height" Value="35"/>
        </Style>

        <Style TargetType="TabItem">
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Padding" Value="20,10"/>
        </Style>
    </UserControl.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="⚙️ Configuración de Maestros" FontSize="20" FontWeight="Bold" Foreground="#37474F" Margin="0,0,0,15"/>

        <TabControl Grid.Row="1" BorderThickness="0" Background="Transparent">

            <TabItem Header="📦 Categorías">
                <Grid Margin="0,15,0,0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="20"/>
                        <ColumnDefinition Width="300"/>
                    </Grid.ColumnDefinitions>

                    <DataGrid Grid.Column="0" ItemsSource="{Binding Categorias}" SelectedItem="{Binding CategoriaSeleccionada}"
                              AutoGenerateColumns="False" IsReadOnly="True" CanUserAddRows="False"
                              GridLinesVisibility="Horizontal" HorizontalGridLinesBrush="#EEEEEE" BorderThickness="1" BorderBrush="#E0E0E0">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="50"/>
                            <DataGridTextColumn Header="Nombre de Categoría" Binding="{Binding Nombre}" Width="*"/>
                        </DataGrid.Columns>
                    </DataGrid>

                    <Border Grid.Column="2" Background="#F5F7FA" CornerRadius="5" Padding="20">
                        <StackPanel>
                            <TextBlock Text="Editar / Crear Categoría" FontWeight="Bold" Margin="0,0,0,15" Foreground="#1976D2"/>

                            <TextBlock Text="Nombre" Margin="0,0,0,5" Foreground="Gray"/>
                            <TextBox Text="{Binding NombreCategoria, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,20"/>

                            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                                <Button Command="{Binding NuevaCategoriaCommand}" Content="Limpia" Margin="0,0,10,0" Padding="10,5" Background="Transparent" BorderThickness="0"/>
                                <Button Command="{Binding EliminarCategoriaCommand}" Content="🗑️" Margin="0,0,10,0" Padding="10,5" Background="#FFEBEE" Foreground="Red" BorderThickness="0"/>
                                <Button Command="{Binding GuardarCategoriaCommand}" Content="💾 Guardar" Padding="15,8" Background="#1976D2" Foreground="White" BorderThickness="0">
                                    <Button.Resources>
                                        <Style TargetType="Border">
                                            <Setter Property="CornerRadius" Value="4"/>
                                        </Style>
                                    </Button.Resources>
                                </Button>
                            </StackPanel>
                        </StackPanel>
                    </Border>
                </Grid>
            </TabItem>

            <TabItem Header="⚖️ Unidades de Medida">
                <Grid Margin="0,15,0,0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="20"/>
                        <ColumnDefinition Width="300"/>
                    </Grid.ColumnDefinitions>

                    <DataGrid Grid.Column="0" ItemsSource="{Binding Unidades}" SelectedItem="{Binding UnidadSeleccionada}"
                              AutoGenerateColumns="False" IsReadOnly="True" CanUserAddRows="False"
                              GridLinesVisibility="Horizontal" HorizontalGridLinesBrush="#EEEEEE" BorderThickness="1" BorderBrush="#E0E0E0">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Nombre" Binding="{Binding Nombre}" Width="*"/>
                            <DataGridTextColumn Header="Abrev." Binding="{Binding Abreviatura}" Width="80"/>
                        </DataGrid.Columns>
                    </DataGrid>

                    <Border Grid.Column="2" Background="#F5F7FA" CornerRadius="5" Padding="20">
                        <StackPanel>
                            <TextBlock Text="Editar / Crear Unidad" FontWeight="Bold" Margin="0,0,0,15" Foreground="#388E3C"/>

                            <TextBlock Text="Nombre (ej: Kilogramo)" Margin="0,0,0,5" Foreground="Gray"/>
                            <TextBox Text="{Binding NombreUnidad, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,15"/>

                            <TextBlock Text="Abreviatura (ej: kg)" Margin="0,0,0,5" Foreground="Gray"/>
                            <TextBox Text="{Binding AbrevUnidad, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,20"/>

                            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                                <Button Command="{Binding NuevaUnidadCommand}" Content="Limpia" Margin="0,0,10,0" Padding="10,5" Background="Transparent" BorderThickness="0"/>
                                <Button Command="{Binding EliminarUnidadCommand}" Content="🗑️" Margin="0,0,10,0" Padding="10,5" Background="#FFEBEE" Foreground="Red" BorderThickness="0"/>
                                <Button Command="{Binding GuardarUnidadCommand}" Content="💾 Guardar" Padding="15,8" Background="#388E3C" Foreground="White" BorderThickness="0">
                                    <Button.Resources>
                                        <Style TargetType="Border">
                                            <Setter Property="CornerRadius" Value="4"/>
                                        </Style>
                                    </Button.Resources>
                                </Button>
                            </StackPanel>
                        </StackPanel>
                    </Border>
                </Grid>
            </TabItem>

        </TabControl>
    </Grid>
</UserControl>