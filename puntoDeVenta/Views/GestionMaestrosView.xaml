<UserControl x:Class="puntoDeVenta.Views.GestionMaestrosView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" d:DesignHeight="500" d:DesignWidth="800"
             Background="White">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Gestión de Maestros" FontSize="22" FontWeight="Bold" Foreground="#3F51B5" Margin="0,0,0,20"/>

        <TabControl Grid.Row="1" BorderThickness="0" Background="Transparent">
            <TabControl.Resources>
                <Style TargetType="TabItem">
                    <Setter Property="FontSize" Value="14"/>
                    <Setter Property="Padding" Value="15,10"/>
                    <Setter Property="FontWeight" Value="SemiBold"/>
                </Style>
            </TabControl.Resources>

            <TabItem Header="📂 Categorías">
                <Grid Margin="0,20,0,0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="300"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <StackPanel Grid.Column="0" Margin="0,0,20,0">
                        <TextBlock Text="Nueva Categoría" FontWeight="Bold" Margin="0,0,0,10"/>
                        <TextBox x:Name="txtNombreCat" Height="35" VerticalContentAlignment="Center" Padding="5,0"/>
                        <Button Content="GUARDAR" Click="BtnGuardarCat_Click" 
                                Background="#4CAF50" Foreground="White" Height="40" Margin="0,10,0,0" FontWeight="Bold"/>
                        <TextBlock Text="* Selecciona una de la lista para eliminar" FontSize="11" Foreground="Gray" Margin="0,10,0,0" TextWrapping="Wrap"/>
                    </StackPanel>

                    <DataGrid x:Name="gridCategorias" Grid.Column="1" AutoGenerateColumns="False" IsReadOnly="True" CanUserAddRows="False">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="50"/>
                            <DataGridTextColumn Header="Nombre" Binding="{Binding Nombre}" Width="*"/>
                            <DataGridTemplateColumn Width="100">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <Button Content="Eliminar" Click="BtnEliminarCat_Click" Background="#EF5350" Foreground="White" Padding="5,0"/>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                        </DataGrid.Columns>
                    </DataGrid>
                </Grid>
            </TabItem>

            <TabItem Header="⚖️ Unidades de Medida">
                <Grid Margin="0,20,0,0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="300"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <StackPanel Grid.Column="0" Margin="0,0,20,0">
                        <TextBlock Text="Nueva Unidad" FontWeight="Bold" Margin="0,0,0,10"/>

                        <TextBlock Text="Nombre (ej: Kilogramo)" FontSize="12" Foreground="Gray"/>
                        <TextBox x:Name="txtNombreUni" Height="35" VerticalContentAlignment="Center" Padding="5,0" Margin="0,0,0,10"/>

                        <TextBlock Text="Abreviatura (ej: kg)" FontSize="12" Foreground="Gray"/>
                        <TextBox x:Name="txtAbreviaUni" Height="35" VerticalContentAlignment="Center" Padding="5,0"/>

                        <Button Content="GUARDAR" Click="BtnGuardarUni_Click" 
                                Background="#4CAF50" Foreground="White" Height="40" Margin="0,10,0,0" FontWeight="Bold"/>
                    </StackPanel>

                    <DataGrid x:Name="gridUnidades" Grid.Column="1" AutoGenerateColumns="False" IsReadOnly="True" CanUserAddRows="False">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="50"/>
                            <DataGridTextColumn Header="Nombre" Binding="{Binding Nombre}" Width="*"/>
                            <DataGridTextColumn Header="Abr." Binding="{Binding Abreviatura}" Width="60"/>
                            <DataGridTemplateColumn Width="100">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <Button Content="Eliminar" Click="BtnEliminarUni_Click" Background="#EF5350" Foreground="White" Padding="5,0"/>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                        </DataGrid.Columns>
                    </DataGrid>
                </Grid>
            </TabItem>
        </TabControl>
    </Grid>
</UserControl>