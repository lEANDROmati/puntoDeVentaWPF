<Window x:Class="puntoDeVenta.Views.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Bienvenido al Sistema" Height="450" Width="750"
        WindowStartupLocation="CenterScreen" ResizeMode="NoResize" 
        WindowStyle="None" AllowsTransparency="True" Background="Transparent"
        MouseDown="Window_MouseDown">

    <Border CornerRadius="15" Background="White">
        <Border.Effect>
            <DropShadowEffect BlurRadius="20" Color="#B0BEC5" Opacity="0.4" ShadowDepth="5"/>
        </Border.Effect>

        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="280"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Border Grid.Column="0" CornerRadius="15,0,0,15">
                <Border.Background>
                    <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                        <GradientStop Color="#3F51B5" Offset="0.0"/>
                        <GradientStop Color="#303F9F" Offset="1.0"/>
                    </LinearGradientBrush>
                </Border.Background>

                <StackPanel VerticalAlignment="Center" Margin="30">
                    <Border Width="74" Height="72" Background="White" CornerRadius="35" Margin="0,0,0,20">
                        <TextBlock Text="simple" FontSize="24" FontWeight="Bold" Foreground="#3F51B5" 
                                   HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Border>

                    <TextBlock Text="SimplePOS" Foreground="White" FontSize="22" FontWeight="Bold" 
                               HorizontalAlignment="Center" Margin="0,0,0,10"/>

                    <Rectangle Height="2" Width="40" Fill="White" Opacity="0.5" Margin="0,0,0,20"/>

                    <TextBlock Text="Manejá tu negocio sin complicaciones: Cobrá rápido, controlá tu stock y mirá tus ganancias al instante." 
                               Foreground="White" FontSize="14" TextWrapping="Wrap" TextAlignment="Center" 
                               Opacity="0.9" LineHeight="20"/>
                </StackPanel>
            </Border>

            <Grid Grid.Column="1">
                <Button Click="BtnCerrar_Click" Width="30" Height="30" HorizontalAlignment="Right" VerticalAlignment="Top" Margin="15"
                        Background="Transparent" BorderThickness="0" Cursor="Hand">
                    <TextBlock Text="✕" FontSize="16" Foreground="#90A4AE"/>
                    <Button.Template>
                        <ControlTemplate TargetType="Button">
                            <Border Background="{TemplateBinding Background}">
                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                        </ControlTemplate>
                    </Button.Template>
                </Button>

                <StackPanel VerticalAlignment="Center" Margin="50,0">

                    <TextBlock Text="¡Hola de nuevo! 👋" FontSize="24" FontWeight="Bold" Foreground="{StaticResource BrushTextoOscuro}" Margin="0,0,0,5"/>
                    <TextBlock Text="Ingresa tus credenciales para continuar" FontSize="14" Foreground="Gray" Margin="0,0,0,30"/>

                    <TextBlock Text="USUARIO" FontWeight="SemiBold" Foreground="#78909C" FontSize="11" Margin="5,0,0,5"/>
                    <TextBox x:Name="txtUsuario" Height="45" FontSize="14" Padding="10,0" VerticalContentAlignment="Center" 
                             BorderBrush="#CFD8DC" Background="#F5F7FA" Margin="0,0,0,20">
                        <TextBox.Resources>
                            <Style TargetType="Border">
                                <Setter Property="CornerRadius" Value="8"/>
                            </Style>
                        </TextBox.Resources>
                    </TextBox>

                    <TextBlock Text="CONTRASEÑA" FontWeight="SemiBold" Foreground="#78909C" FontSize="11" Margin="5,0,0,5"/>
                    <PasswordBox x:Name="txtPassword" Height="45" FontSize="14" Padding="10,0" VerticalContentAlignment="Center"
                                 BorderBrush="#CFD8DC" Background="#F5F7FA" Margin="0,0,0,30">
                        <PasswordBox.Resources>
                            <Style TargetType="Border">
                                <Setter Property="CornerRadius" Value="8"/>
                            </Style>
                        </PasswordBox.Resources>
                    </PasswordBox>

                    <Button x:Name="btnIngresar" Content="INGRESAR AL SISTEMA" Click="BtnLogin_Click"
                            Height="50" Background="{StaticResource BrushPrimario}" Foreground="White"
                            FontSize="14" FontWeight="Bold" Cursor="Hand" BorderThickness="0">
                        <Button.Template>
                            <ControlTemplate TargetType="Button">
                                <Border x:Name="bdr" Background="{TemplateBinding Background}" CornerRadius="25">
                                    <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                </Border>
                                <ControlTemplate.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter TargetName="bdr" Property="Background" Value="#303F9F"/>
                                    </Trigger>
                                </ControlTemplate.Triggers>
                            </ControlTemplate>
                        </Button.Template>
                    </Button>

                    <TextBlock Text="Versión 1.0 - 2025" HorizontalAlignment="Center" Margin="0,20,0,0" FontSize="11" Foreground="#CFD8DC"/>
                </StackPanel>
            </Grid>
        </Grid>
    </Border>
</Window>