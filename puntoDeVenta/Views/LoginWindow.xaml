<Window x:Class="puntoDeVenta.Views.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Login" Height="400" Width="300"
        WindowStartupLocation="CenterScreen" ResizeMode="NoResize" WindowStyle="None"
        Background="Transparent" AllowsTransparency="True">

    <Border Background="White" CornerRadius="15" BorderBrush="#3F51B5" BorderThickness="1">
        <Border.Effect>
            <DropShadowEffect BlurRadius="10" Opacity="0.3"/>
        </Border.Effect>

        <Grid Margin="30">
            <StackPanel VerticalAlignment="Center">
                <TextBlock Text="🔐" FontSize="40" HorizontalAlignment="Center" Margin="0,0,0,10"/>
                <TextBlock Text="INICIAR SESIÓN" FontSize="18" FontWeight="Bold" Foreground="#3F51B5" HorizontalAlignment="Center" Margin="0,0,0,20"/>

                <TextBlock Text="Usuario:" FontWeight="SemiBold"/>
                <TextBox x:Name="txtUser" Height="30" FontSize="14" Margin="0,5,0,15"/>

                <TextBlock Text="Contraseña:" FontWeight="SemiBold"/>
                <PasswordBox x:Name="txtPass" Height="30" FontSize="14" Margin="0,5,0,20"/>

                <Button Content="INGRESAR" Click="BtnIngresar_Click" IsDefault="True"
                        Height="40" Background="#3F51B5" Foreground="White" FontWeight="Bold"/>

                <Button Content="Salir" Click="BtnSalir_Click" IsCancel="True"
                        Height="30" Margin="0,10,0,0" Background="Transparent" Foreground="Red" BorderThickness="0"/>
            </StackPanel>
        </Grid>
    </Border>
</Window>