<UserControl x:Class="puntoDeVenta.Views.DashboardView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" d:DesignHeight="600" d:DesignWidth="900">

    <Grid Margin="30">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="0,0,0,30">
            <TextBlock Text="Resumen General" FontSize="26" FontWeight="Bold" Foreground="{StaticResource BrushTextoOscuro}"/>
            <TextBlock Text="Vistazo rápido a la actividad de hoy" FontSize="14" Foreground="Gray" Margin="0,5,0,0"/>
        </StackPanel>

        <Grid Grid.Row="1" Margin="0,0,0,30">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="20"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="20"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Border Grid.Column="0" Background="White" CornerRadius="15" Padding="20">
                <Border.Effect>
                    <DropShadowEffect BlurRadius="15" Color="#CFD8DC" Opacity="0.4" ShadowDepth="2"/>
                </Border.Effect>
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <StackPanel>
                        <TextBlock Text="Ventas de Hoy" FontSize="14" Foreground="Gray" FontWeight="SemiBold"/>
                        <TextBlock Text="$ 15,240.00" FontSize="28" FontWeight="Bold" Foreground="{StaticResource BrushPrimario}" Margin="0,5,0,0"/>
                        <TextBlock Text="+ 12% vs ayer" FontSize="12" Foreground="{StaticResource BrushVerdeExito}" Margin="0,5,0,0"/>
                    </StackPanel>
                    <Border Grid.Column="1" Width="50" Height="50" CornerRadius="25" Background="#E8EAF6" VerticalAlignment="Top">
                        <TextBlock Text="💰" FontSize="24" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Border>
                </Grid>
            </Border>

            <Border Grid.Column="2" Background="White" CornerRadius="15" Padding="20">
                <Border.Effect>
                    <DropShadowEffect BlurRadius="15" Color="#CFD8DC" Opacity="0.4" ShadowDepth="2"/>
                </Border.Effect>
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <StackPanel>
                        <TextBlock Text="Tickets Emitidos" FontSize="14" Foreground="Gray" FontWeight="SemiBold"/>
                        <TextBlock Text="34" FontSize="28" FontWeight="Bold" Foreground="{StaticResource BrushTextoOscuro}" Margin="0,5,0,0"/>
                        <TextBlock Text="5 en la última hora" FontSize="12" Foreground="Gray" Margin="0,5,0,0"/>
                    </StackPanel>
                    <Border Grid.Column="1" Width="50" Height="50" CornerRadius="25" Background="#FFF3E0" VerticalAlignment="Top">
                        <TextBlock Text="🧾" FontSize="24" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Border>
                </Grid>
            </Border>

            <Border Grid.Column="4" Background="White" CornerRadius="15" Padding="20">
                <Border.Effect>
                    <DropShadowEffect BlurRadius="15" Color="#CFD8DC" Opacity="0.4" ShadowDepth="2"/>
                </Border.Effect>
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <StackPanel>
                        <TextBlock Text="Productos Stock Bajo" FontSize="14" Foreground="Gray" FontWeight="SemiBold"/>
                        <TextBlock Text="3" FontSize="28" FontWeight="Bold" Foreground="{StaticResource BrushRojoAlerta}" Margin="0,5,0,0"/>
                        <TextBlock Text="Requieren reposición" FontSize="12" Foreground="{StaticResource BrushRojoAlerta}" Margin="0,5,0,0"/>
                    </StackPanel>
                    <Border Grid.Column="1" Width="50" Height="50" CornerRadius="25" Background="#FFEBEE" VerticalAlignment="Top">
                        <TextBlock Text="📦" FontSize="24" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Border>
                </Grid>
            </Border>
        </Grid>

        <Border Grid.Row="2" Background="White" CornerRadius="15" Padding="20">
            <Border.Effect>
                <DropShadowEffect BlurRadius="15" Color="#CFD8DC" Opacity="0.4" ShadowDepth="2"/>
            </Border.Effect>
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <TextBlock Text="Rendimiento Semanal" FontSize="18" FontWeight="Bold" Foreground="{StaticResource BrushTextoOscuro}"/>

                <Grid Grid.Row="1" Margin="0,20,0,0" VerticalAlignment="Bottom">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <StackPanel Grid.Column="0" VerticalAlignment="Bottom" Margin="10,0">
                        <Border Height="100" Background="#E0E0E0" CornerRadius="5,5,0,0"/>
                        <TextBlock Text="Lun" HorizontalAlignment="Center" Margin="0,5,0,0" Foreground="Gray"/>
                    </StackPanel>
                    <StackPanel Grid.Column="1" VerticalAlignment="Bottom" Margin="10,0">
                        <Border Height="140" Background="#E0E0E0" CornerRadius="5,5,0,0"/>
                        <TextBlock Text="Mar" HorizontalAlignment="Center" Margin="0,5,0,0" Foreground="Gray"/>
                    </StackPanel>
                    <StackPanel Grid.Column="2" VerticalAlignment="Bottom" Margin="10,0">
                        <Border Height="120" Background="#E0E0E0" CornerRadius="5,5,0,0"/>
                        <TextBlock Text="Mié" HorizontalAlignment="Center" Margin="0,5,0,0" Foreground="Gray"/>
                    </StackPanel>
                    <StackPanel Grid.Column="3" VerticalAlignment="Bottom" Margin="10,0">
                        <Border Height="180" Background="{StaticResource BrushPrimario}" CornerRadius="5,5,0,0"/>
                        <TextBlock Text="Jue (Hoy)" HorizontalAlignment="Center" Margin="0,5,0,0" FontWeight="Bold" Foreground="{StaticResource BrushPrimario}"/>
                    </StackPanel>
                    <StackPanel Grid.Column="4" VerticalAlignment="Bottom" Margin="10,0">
                        <Border Height="20" Background="#F5F5F5" CornerRadius="5,5,0,0"/>
                        <TextBlock Text="Vie" HorizontalAlignment="Center" Margin="0,5,0,0" Foreground="Gray"/>
                    </StackPanel>
                </Grid>
            </Grid>
        </Border>
    </Grid>
</UserControl>