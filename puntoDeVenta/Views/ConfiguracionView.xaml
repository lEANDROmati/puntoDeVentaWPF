<UserControl x:Class="puntoDeVenta.Views.ConfiguracionView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:vm="clr-namespace:puntoDeVenta.ViewModels"
             mc:Ignorable="d" d:DesignHeight="600" d:DesignWidth="900">

    <d:UserControl.DataContext>
        <vm:ConfiguracionViewModel/>
    </d:UserControl.DataContext>

    <Grid Margin="30">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="0,0,0,30">
            <TextBlock Text="Configuración del Sistema" FontSize="26" FontWeight="Bold" Foreground="{StaticResource BrushTextoOscuro}"/>
            <TextBlock Text="Personaliza el comportamiento de tu Punto de Venta" FontSize="14" Foreground="Gray" Margin="0,5,0,0"/>
        </StackPanel>

        <Border Grid.Row="1" Background="White" CornerRadius="15" Padding="40" HorizontalAlignment="Left" Width="600">
            <Border.Effect>
                <DropShadowEffect BlurRadius="15" Color="#CFD8DC" Opacity="0.4" ShadowDepth="2"/>
            </Border.Effect>

            <StackPanel>

                <TextBlock Text="Datos del Negocio" FontSize="18" FontWeight="Bold" Foreground="{StaticResource BrushPrimario}" Margin="0,0,0,15"/>

                <TextBlock Text="Nombre del Comercio:" FontWeight="SemiBold" Foreground="#546E7A" Margin="0,0,0,5"/>
                <TextBox Text="{Binding NombreNegocio}" Height="40" FontSize="16" VerticalContentAlignment="Center" Padding="10,0" BorderBrush="#CFD8DC">
                    <TextBox.Resources>
                        <Style TargetType="{x:Type Border}">
                            <Setter Property="CornerRadius" Value="5"/>
                        </Style>
                    </TextBox.Resources>
                </TextBox>
                <TextBlock Text="Este nombre aparecerá en la cabecera y en los tickets." FontSize="12" Foreground="#90A4AE" Margin="0,5,0,20"/>

                <Separator Background="#ECEFF1" Margin="0,0,0,20"/>

                <TextBlock Text="Preferencias de Venta" FontSize="18" FontWeight="Bold" Foreground="{StaticResource BrushPrimario}" Margin="0,0,0,15"/>

                <CheckBox IsChecked="{Binding ImprimirTicket}" Margin="0,0,0,15">
                    <CheckBox.LayoutTransform>
                        <ScaleTransform ScaleX="1.2" ScaleY="1.2"/>
                    </CheckBox.LayoutTransform>
                    <TextBlock Text="Imprimir Ticket automáticamente al cobrar" FontSize="14" Margin="10,0,0,0" VerticalAlignment="Center"/>
                </CheckBox>

                <CheckBox IsChecked="{Binding UsarControlCaja}" Margin="0,0,0,20">
                    <CheckBox.LayoutTransform>
                        <ScaleTransform ScaleX="1.2" ScaleY="1.2"/>
                    </CheckBox.LayoutTransform>
                    <TextBlock Text="Exigir 'Apertura de Caja' antes de vender" FontSize="14" Margin="10,0,0,0" VerticalAlignment="Center"/>
                </CheckBox>

                <Separator Background="#ECEFF1" Margin="0,0,0,20"/>

                <Button Content="GUARDAR CAMBIOS" Command="{Binding GuardarCambiosCommand}"
                        Background="{StaticResource BrushPrimario}" Foreground="White"
                        Height="50" Width="200" HorizontalAlignment="Right"
                        FontSize="14" FontWeight="Bold" BorderThickness="0" Cursor="Hand">
                    <Button.Template>
                        <ControlTemplate TargetType="Button">
                            <Border x:Name="border" Background="{TemplateBinding Background}" CornerRadius="25">
                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter TargetName="border" Property="Background" Value="#303F9F"/>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Button.Template>
                </Button>

            </StackPanel>
        </Border>
    </Grid>
</UserControl>